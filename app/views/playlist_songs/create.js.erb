$('.modal-body').html('<%= j render "songs/add_playlist" %>');
$(".notify-text").html("<%= flash.now[:notice] %>");
$('#notify').removeClass('d-none');
$('#add_playlist').modal('hide');
setTimeout(function(){
  $('.notify-text').html('');
  $('#notify').addClass('d-none');
},4000)
