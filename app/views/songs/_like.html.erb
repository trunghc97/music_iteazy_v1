<%= form_with url: likeds_path, remote: true do |f| %>
  <%= hidden_field_tag :user_id, current_user.id %>
  <%= hidden_field_tag :song_id, @song.id %>
  <%= f.submit t(".btn_like"), class: "btn btn-primary" %>
<% end %>
