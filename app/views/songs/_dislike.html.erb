<%= form_with url: liked_path(@song.liked), method: :delete, remote: true do |f| %>
  <%= hidden_field_tag :user_id, current_user.id %>
  <%= hidden_field_tag :song_id, @song.id %>
  <%= f.submit t(".btn_dislike"), class: "btn btn-danger" %>
<% end %>
